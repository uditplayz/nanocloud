# Example environment file for Cloudrave backend
# Copy this file to `.env` and fill in real secrets. Do NOT commit `.env`.

# Google GenAI / Gemini API key (server-side only)
GEMINI_API_KEY=

# Optional: MongoDB connection string for persistence
MONGO_URI=

# JWT secret for signing tokens
JWT_SECRET=

# AWS (if using S3 features in routes/files.js)
# AWS_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET_NAME=
PORT=5000